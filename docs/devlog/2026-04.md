# Development Log — April 2026

Newest entries first.

## 2026-04-02 — Landing Page Screenshots + Demo Form Backend

### Done

- Audited all 7 landing page screenshots against their associated copy — found 5 mismatches (empty states, wrong pages, wrong audience)
- Captured 4 new screenshots via Chrome DevTools MCP: editor submissions queue, writer submissions, reading queue triage mode, deep-read mode with ManuscriptRenderer
- Replaced all mismatched screenshots in landing-hero.tsx and landing-features.tsx with updated alt text
- Removed 5 unused screenshots, kept 2 that were correct (manuscripts-list.png, slate-pipeline.png)
- Created `demo_requests` table (no RLS — public pre-signup data) with migration 0066
- Built `POST /v1/public/demo-requests` public Fastify endpoint: Zod validation, DB insert, confirmation + team notification emails via SMTP adapter, per-endpoint rate limit (5/IP/hour via ioredis sorted sets)
- Added `DEMO_NOTIFY_EMAIL` env var for team notification recipient
- Wired frontend demo form to API: fetch POST, loading state, error handling with toast feedback
- Codex plan review: 2 Important findings addressed (wrong sim-sub route path, missing env wiring in main.ts)

### Decisions

- Demo requests table has no RLS: not org-scoped, public pre-signup data uses superuser pool
- Email sending is best-effort: SMTP failures logged but don't fail the request
- Per-endpoint rate limit (5/hr) on top of global IP rate limit (60/min): prevents form spam specifically
- ioredis uses lowercase method names (`zadd`, `zcard`, `zremrangebyscore`) — caught by type-check

---

## 2026-04-01 — Marketing Landing Page

### Done

- Replaced placeholder landing page with polished marketing page targeting editorial directors
- Seven sections: sticky header (logotype SVG, nav, CTAs), hero with overlapping writer+editor screenshots, "Two Sides of Every Submission" split feature section, three differentiator deep-dives (typography rendering, sim-sub tracking, full publication lifecycle), deployment options (self-hosted vs managed hosting), demo request form (react-hook-form + zod), footer
- Dark theme throughout via `.dark` wrapper div — activates dark CSS tokens (navy bg, cream text, copper accents) without changing global theme state
- Copied 7 product screenshots from QA captures to `public/screenshots/` for use as hero/feature visuals
- Created `useInView` Intersection Observer hook for scroll-triggered fade-in animations
- Used `next/image` for all screenshots (lazy loading, format optimization, priority on hero images)
- Demo form with zod validation and sonner toast feedback (TODO: wire backend endpoint)
- Preserved existing OIDC auth guard — authenticated users still bypass to `/submissions`

### Decisions

- `.dark` wrapper div over global theme change: landing page is always dark, rest of app respects user preference
- `next/image` over plain `<img>`: first use in codebase, justified by multiple large screenshots
- No mobile hamburger menu in v1: nav links hide below md, CTAs remain visible
- Copper accent restricted to CTAs and meaningful emphasis only (per brand guide)

---

## 2026-04-01 — Full Manual QA Audit

### Done

- Systematic manual QA walkthrough of every major page and feature in the app
  - Tested: Landing, Instance Identity, Auth flow, Org creation, My Submissions, Editor Dashboard, All Submissions, Submission Detail (with queue nav), Forms, Writer Workspace, Slate Production Dashboard, Business Dashboard, Operations Dashboard, Federation Admin, Org Settings, Notifications
  - Tested dark mode across multiple pages — good contrast, proper theming
  - Tested mobile viewport (390px) — hamburger menu, sidebar collapse, horizontal scroll tables, responsive landing page
- Fixed Bug #1: Instance Identity page showed misleading "API server may be unavailable" error when federation was disabled
  - Root cause: `.well-known/colophony` route not registered when `FEDERATION_ENABLED=false` → 404, but frontend only handled 503
  - Fix: Register discovery routes unconditionally in `main.ts`; add env fast-path in `federationService.getInstanceMetadata()` and `resolveWebFinger()` before DB queries
- Fixed Bug #2: `db:reset` lost all SQL functions, triggers, and grants
  - Root cause: Script dropped `public` schema but preserved `drizzle` schema (migration journal), so `db:migrate` thought all migrations were applied and silently no-oped; `drizzle-kit push` fallback only created tables
  - Fix: Drop `drizzle` schema alongside `public` so migrations run from clean state; eliminated fragile `push` fallback

### Decisions

- Always register `.well-known/colophony` route regardless of federation flag — cleaner than frontend workaround, and WebFinger benefits from same pattern
- Drop `drizzle` schema in `db:reset` — root cause fix rather than working around the Drizzle silent no-op quirk

---

## 2026-04-01 — Demo Prep: Withdrawal Cascade, Seed Data, Badge Animation

### Done

- Built withdrawal cascade feature: cross-org batch withdrawal when a manuscript is accepted at one magazine
  - API: `findSiblingSubmissions()` (superuser cross-org lookup), `withdrawCascadeAsOwner()` (batch withdraw with history)
  - tRPC: `submissions.findSiblings` and `submissions.withdrawCascade` via `userProcedure` (cross-org)
  - UI: notification card on accepted submissions showing pending siblings, confirm dialog with courteous withdrawal note
- Created demo seed script (`seed-demo.ts`): 3 fictional magazines (Meridian Review, Saltwater Quarterly, Glass River Literary), writer with 3 manuscripts (poetry with non-trivial formatting, fiction, nonfiction), simultaneous submissions across magazines, editorial review (votes, discussion), publication pipeline (contract, issue assembly), contributor payments
- Added "Accepted" badge entrance animation (`animate-status-enter`): scale-up + green glow pulse, `animated` prop on StatusBadge and WriterStatusBadge
- Attempted Playwright demo recording harness — abandoned due to persistent auth interception issues (route interception unreliable across sequential browser contexts, deterministic failure pattern)

### Decisions

- `userProcedure` (not `orgProcedure`) for withdrawal cascade — writer withdraws across multiple orgs
- Superuser `db` pool for sibling submission lookup — same pattern as simsub service, scoped by `submitterId`
- Demo video will need manual screen recording — Playwright auth interception creates new browser contexts per test, and fake OIDC tokens leak before route handlers are established regardless of page vs context-level interception

---

## 2026-04-01 — Branding Wire-Up (Launch Prep)

### Done

- Created favicon rasterization script (`scripts/generate-brand-assets.ts`) using Playwright + sharp
- Generated branded favicons: ICO (star-only@16 + C+star@32), apple-icon@180, manifest icons (192, 512)
- Dynamic OG image via Satori (`opengraph-image.tsx`): navy bg, copper star bezier path, Playfair Display Bold wordmark, cream tagline
- Root layout metadata: title template (`%s | Colophony`), OG/Twitter cards, icon declarations, theme color
- Web manifest (`manifest.ts`): PWA-lite with brand colors and icon references
- Robots.txt: 9 disallowed dashboard routes, allowed embed/identity public pages
- Sitemap: minimal (/ and /identity only — everything else auth-gated)
- Portable logo SVGs in `public/logos/` with embedded base64 woff2 fonts (no external font dependencies)
- Wired `NEXT_PUBLIC_APP_URL` into Dockerfile + `docker-compose.prod.yml` (derived from `DOMAIN`)
- Fixed identity page title duplication with new template
- Added brand asset source files to repo (`docs/branding/` — 5 SVGs + spec doc)
- Codex plan review ran twice (pre and post user feedback); all Important findings addressed in plan

### Decisions

- `NEXT_PUBLIC_APP_URL` derives from existing `DOMAIN` env var — not a new source of truth
- 16x16 favicon uses star-only (C+star illegible at that size); 32x32 uses full C+star logomark
- Portable SVGs via embedded base64 woff2 fonts, not glyph path extraction — simpler, same portability result
- Dynamic OG route via Satori instead of static PNG — star bezier renders correctly, simpler to maintain
- Description: "Open-source editorial workflow for literary magazines — from submission to publication."

---

## 2026-04-01 — UX Clarity Audit

### Done

- Full dashboard UX clarity audit: reviewed ~75 pages for missing explanatory text, tooltips, and contextual help
- Created `InfoTooltip` component (`info-tooltip.tsx`) — reusable info icon with Radix tooltip, WCAG AA keyboard accessible
- Added global `TooltipProvider` (300ms delay) to dashboard layout
- Workspace pages: updated subtitles and added InfoTooltips on workspace dashboard, stats cards, portfolio (group-by-piece tooltip), correspondence (arrow legend), writer analytics (chart descriptions), sim-sub groups
- Slate/admin pages: production dashboard + stats card tooltips, contract/CMS/webhook/period subtitle and empty state copy, collections subtitle
- Navigation: extended `NavItem` with optional `description` field, added descriptions to 10 non-obvious sidebar items, sidebar tooltips with 450ms local delay, command palette shows searchable descriptions
- Editorial: triage list keyboard hint bar (j/k/r/?) in footer with brand-palette kbd styling, editor dashboard card descriptions enhanced
- Fixed 3 test failures from changes (TooltipProvider wrappers, regex text matcher)
- Codex plan review ran twice (pre- and post-feedback revision); both passed with minor issues addressed

### Decisions

- 450ms sidebar tooltip delay (vs 300ms global) to avoid annoying power users on frequent navigation
- Descriptions only on non-obvious nav items (10 of ~30) — skip Dashboard, Manuscripts, Settings, etc.
- Warm brand-palette `<kbd>` styling for triage hints (sidebar-accent bg, sidebar-foreground text)
- Removed patronizing "most journals accept 2-5%" copy from Rejected stat card per user feedback
- Trimmed sim-sub subtitle to two sentences — writers already know the concept
- Correspondence subtitle softened: removed "externally-tracked submissions won't appear here" after Codex found the service may include external submission data

---

## 2026-04-01 — A11y Quick Wins + db:reset Fix

### Done

- Skip navigation link: sr-only "Skip to main content" link in dashboard layout, visible on Tab focus, targets `#main-content` on `<main>` element
- Command palette focus restoration: captures `activeElement` on open via `useRef`, restores focus on close (fixes focus going to `<body>` when using context-based open/close instead of Radix DialogTrigger)
- Rewrote `scripts/db-reset.sh`: uses `DROP SCHEMA public CASCADE` + `drizzle-kit push --force` + full grant restoration instead of `docker compose down -v`. Preserves Zitadel database and postgres volume
- Codex plan review caught 3 Important findings on db:reset: Drizzle migrate() silent no-op, missing role grants, Zitadel separate database assumption — all addressed before implementation
- Codex branch review caught 2 findings: focus capture timing (P2, useEffect too late — moved to synchronous setOpen wrapper) and migration-defined functions lost by drizzle-kit push (P1 — switched to migrate() primary with push fallback)
- Updated CLAUDE.md quirks table and backlog (3 items closed)

### Decisions

- `drizzle-kit push --force` over `migrate()` after schema drop — `migrate()` silently no-ops after `DROP SCHEMA CASCADE` (documented quirk in packages/db/CLAUDE.md)
- Full `init-db.sh` grant restoration in db-reset: `app_user` USAGE + DML, `audit_writer` USAGE, default privileges, restricted table revocations (delete-only and select-only tables)

---

## 2026-04-01 — Visual QA + Accessibility Remediation

### Done

- Visual QA walkthrough: 110 screenshots across 55 pages (light + dark mode) via Chrome DevTools MCP
- WCAG AA contrast fixes on 6 status color tokens: status-info (55→36%), status-warning (38→30%), status-error (47→44%), status-active (52→38%), status-held (48→40%), muted-foreground (44→36%)
- Breadcrumb preheader: removed /60 opacity modifier for contrast compliance
- FilterTabs component: proper HTML semantics for filter-only tab UIs (no invalid aria-controls); migrated 17 components from Radix Tabs
- Lighthouse accessibility: 90 → 100 (0 failures, was 2: contrast + ARIA)
- Manual a11y audit: landmarks, keyboard nav, focus management, heading hierarchy — all passing
- Defense-in-depth fix: `contributor.service.ts:listPublications()` now requires orgId with join-through-contributors verification
- Staging seed data for 11 tables (Tracks 12-14): contributors, contributor_publications, rights_agreements, payment_transactions, contest_groups, simsub_groups, simsub_group_submissions, portfolio_entries, reader_feedback, workspace_collections, workspace_items
- Documented `db:reset` Zitadel eventstore destruction quirk (CLAUDE.md + backlog)

### Decisions

- FilterTabs over Radix Tabs forceMount: avoids unnecessary API calls from mounting all tab content panels simultaneously
- Status colors darkened independently from --accent: brand copper unchanged on buttons/links, only --status-active affected
- Breadcrumb preheader uses full muted-foreground instead of /60 opacity — opacity modifiers on already-muted colors compound below AA threshold

---

## 2026-04-01 — Platform Styling Pass (Brand Identity to Code)

### Done

- Replaced default shadcn/ui neutral palette with Colophony brand identity across 70 files
- Theme tokens: Deep Navy, Warm Cream, Copper, Light Parchment mapped to all shadcn/ui CSS variables (light + dark themes)
- Typography: Playfair Display (headings) + Lato (body/UI) replacing Geist; Literata stays ManuscriptRenderer-only
- Dark mode: next-themes integration with ThemeToggle (light/dark/system cycle) in header
- Semantic status colors: 6 CSS variables (success/warning/error/info/active/held) replacing hardcoded Tailwind classes across 51 component files and 4 test files
- ManuscriptRenderer reading themes: light/sepia/dark scoped via data-reading-theme attribute; useReadingTheme hook (useSyncExternalStore + localStorage); ReadingThemeSelector with circular color swatches
- Sidebar refinement: copper left border on active items (replacing background highlight), 80% opacity inactive text, 12px activity group labels
- Breadcrumb: current page segment at full foreground contrast
- Component refinements: outline/ghost button hover to secondary (not copper), warm-tinted card shadows, command palette selection at 15% copper opacity, Playfair Bold Italic on Accept/Reject/Hold decision labels
- Chart colors harmonized with brand family (navy, copper, sage, amber, dusty blue)
- DESIGN_SYSTEM.md Section 10: brand token system documentation (palette, typography, status colors, reading themes, brand rules)
- Codex plan review: 3 Important findings all addressed before implementation (status migration scope expanded 30→47 files, reading theme hook changed from Context/Provider to self-contained, productive friction target corrected to status-transition.tsx)
- All 758 web unit tests passing

### Decisions

- next-themes with `defaultTheme="light"` + `enableSystem`: dark theme is a first-class part of the brand identity, not an afterthought
- Self-contained `useReadingTheme` hook (useSyncExternalStore + localStorage) instead of Context/Provider — reading theme is a global user preference, not component-scoped state
- Outline/ghost button hover uses `bg-secondary` (parchment) instead of `bg-accent` (copper) — copper is never decorative per brand guidelines
- Bold Italic (not Regular Italic) for productive friction on decision buttons — Regular Italic too fragile at 13-14px button-label size
- Chart colors derived from brand family (navy, copper, sage green, warm amber, dusty blue) for visual consistency on analytics dashboards
