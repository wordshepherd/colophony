# Generated by openapi-python-client 0.29.0 on Python 3.12.
#
# The generator version is pinned in the `sdk-check` CI job, which regenerates
# `colophony/` and fails on any diff. Generated output is version-sensitive, so
# bumping the generator requires regenerating and committing the client in the
# same change — update the pin in .github/workflows/ci.yml alongside this note.
project_name_override: colophony-sdk
package_name_override: colophony
